t = 5
c = 2
p = 3
d = 3
i = int(input("enter input:"))
a = (1,2)
b = (t,c,p,d,i)
for i in f"T{a}{b}T{a}{b}T{a}{b}T{a}{b}T{a}{b}T{a}{b}":
  if ({b} == 1):
    T1 = f"{a}" + f"{a}" +f"{a}" +f"{a}" +f"{a}" +f"{a}" 
  if ({b} == 2):
    T2 = f"{a}" + f"{a}" +f"{a}" +f"{a}" +f"{a}" +f"{a}" 
print(T1,":",T2)
